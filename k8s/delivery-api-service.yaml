apiVersion: v1
kind: Service
metadata:
  name: delivery-api-service
  labels:
    app: delivery-api
spec:
  type: LoadBalancer
  selector:
    app: delivery-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
